<h1>Registrar Fabricante </h1>

<% @manufacturer.errors.full_messages.each do |m| %>
  <p><%= m %></p>
<% end %>

<%# <%= form_for @manufacturer do |f| %>  
<%= form_with model: @manufacturer, local: true do |f| %> 
  <%= f.label :name, 'Nome' %>
  <%= f.text_field :name %>
  <%= f.submit 'Enviar' %>
<% end %>